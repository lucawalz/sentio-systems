# Hero Components

Landing page hero section components.

## Usage with Tailark

Add tailark components via shadcn CLI:
```bash
npx shadcn@latest add @tailark/hero-1
```

This will add the hero component to this directory.
